export interface SpotifyNowplayingStorage {
  accessToken: string;
  clientId: string;
  codeVerifier: string;
  expiresAt: number;
  isPremium: boolean;
  refreshToken: string;
  userName: string;
}
